
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: "8080
    ports:
      - "8080:8080"
    volumes:
      - forge_uploads:/app/uploads

volumes:
  forge_uploads: