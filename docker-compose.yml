services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: forge
      POSTGRES_PASSWORD: forge
      POSTGRES_DB: forge
    ports:
      - "5432:5432"
    volumes:
      - forge_pg_data:/var/lib/postgresql/data

volumes:
  forge_pg_data:
